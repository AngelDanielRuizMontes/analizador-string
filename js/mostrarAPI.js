"use strict"

//Función Arrow

const mostrarAPI = resultado => {
    console.log(resultado.quote);
    texto.value = resultado.quote;
}

//Función tradicional

/* function mostrarAPI(resultado) {
    console.log(resultado.quote);
    texto.value = resultado.quote;
} */